


process nano_qc_plot {
    input:
	tuple val(base), path(fq)
    output:
        path(qcdir), topic: 'report'	
    script:
	qcdir="${base}-qc"
        """
        NanoPlot --fastq $fq --outdir $qcdir -p ${base}- 
        """
}
